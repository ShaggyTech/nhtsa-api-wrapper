import { enableFetchMocks } from 'jest-fetch-mock';

// eslint-disable-next-line jest/require-hook
enableFetchMocks();
// const mockData = require('../../src/__mocks__/mockData')
// fetchMock.mockResponse(JSON.stringify({ ...mockData }));
