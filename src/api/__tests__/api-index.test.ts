import { NHTSA, Client } from '../index';

describe('NHTSA Class', () => {
  test('it exists', () => {
    expect(NHTSA).toBeDefined();
  });
});

describe('NHSTA Client', () => {
  test('it exists', () => {
    expect(Client).toBeDefined();
  });
});
